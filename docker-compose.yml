version: '3'
services:
  server:
    build:
      context: ./voting-app-server
    container_name: voting-app-server
    ports:
      - "5000:5000"
    volumes:
      - server-data:/app/data
    depends_on:
      - redis
      - alloy
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - ALLOY_URL=http://alloy:12345
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://tempo:3200
      - GRAPHITE_HOST=graphite  # Add Graphite service name here
      - GRAPHITE_PORT=2003  # If you're sending metrics to Graphite Carbon port
    networks:
      - voting-app-network

  client:
    build:
      context: ./voting-app-client
    container_name: voting-app-client
    ports:
      - "3001:3001"
    volumes:
      - client-data:/usr/share/nginx/html
    networks:
      - voting-app-network

  redis:
    image: "redis:alpine"
    container_name: voting-app-redis
    ports:
      - "6379:6379"
    networks:
      - voting-app-network

  nginx:
    image: nginx:alpine
    container_name: voting-app-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - client
      - server
    networks:
      - voting-app-network

  alloy:
    image: grafana/alloy:v1.0.0
    pull_policy: always
    restart: on-failure
    volumes:
      - ./config.alloy:/etc/alloy/config.alloy
    environment:
      REMOTE_WRITE_HOST: mimir:9090
      LOKI_HOST: loki:3100
      TEMPO_HOST: tempo:4318
      PYROSCOPE_HOST: pyroscope:4040
    depends_on:
      - mimir
      - loki
      - tempo
      - pyroscope
    command:
      - run
      - /etc/alloy/config.alloy
      - --storage.path=/var/lib/alloy/data
      - --server.http.listen-addr=0.0.0.0:12345
      - --stability.level=experimental # Enable all functionality
    ports:
      - "12345:12345"
    networks:
      - voting-app-network

  mimir:
    image: grafana/mimir:2.12.0
    restart: on-failure
    command:
      - -config.file=/etc/mimir-config/mimir-config.yaml
    volumes:
      - ./mimir-config.yaml:/etc/mimir-config/mimir-config.yaml
    ports:
      - "8080:8080"
    networks:
      - voting-app-network

  loki:
    image: grafana/loki:3.0.0
    restart: on-failure
    ports:
      - "3100:3100"
    volumes:
      - ./loki-config.yaml:/etc/grafana-config/loki-config.yaml
    networks:
      - voting-app-network

  tempo:
    image: grafana/tempo:2.4.1
    restart: on-failure
    command:
      - "-storage.trace.backend=local"                  
      - "-storage.trace.local.path=/tmp/tempo/traces"
      - "-storage.trace.wal.path=/tmp/tempo/wal"        
      - "-auth.enabled=false"
      - "-server.http-listen-port=3200"
    ports:
      - "3200:3200"
      - "4318:4318"
    networks:
      - voting-app-network

  pyroscope:
    image: grafana/pyroscope:1.5.0
    restart: on-failure
    ports:
      - "4040:4040"
    networks:
      - voting-app-network


  graphite:
    image: graphiteapp/graphite-statsd:latest
    container_name: graphite
    ports:
      - "8081:80"       # Graphite web interface
      - "2003-2004:2003-2004"  # Graphite Carbon ports
      - "8125:8125/udp"  # StatsD UDP port
    environment:
      - GRAPHITE_TIME_ZONE=UTC
    volumes:
      - graphite-data:/opt/graphite/storage
    networks:
      - voting-app-network

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    command:
      - --config=/etc/grafana-config/grafana.ini
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./grafana.ini:/etc/grafana-config/grafana.ini
    depends_on:
      - mimir
      - loki
      - tempo
    networks:
      - voting-app-network


  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: otel-collector
    command:
      - "--config=/etc/otelcol/otel.yaml"
    volumes:
      - ./otel.yaml:/etc/otelcol/otel.yaml  # Mount your OTEL Collector config file
    ports:
      - "4317:4317"  # OTLP (Traces and Metrics) - default
      - "55680:55680"  # gRPC endpoint for OTLP
    networks:
      - voting-app-network

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yaml:/etc/prometheus/prometheus.yaml
    command:
      - "--config.file=/etc/prometheus/prometheus.yaml"
    networks:
      - voting-app-network

  promtail:
    image: grafana/promtail:latest
    container_name: promtail
    volumes:
      - ./promtail.yaml:/etc/promtail/promtail.yaml
    command: -config.file=/etc/promtail/promtail.yaml
    networks:
      - voting-app-network

networks:
  voting-app-network:
    driver: bridge

volumes:
  server-data:
  client-data:
  graphite-data:
